<!DOCTYPE html>
<html>
<head>
    <title>Active Sessions - Admin Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <h1 class="mb-4">Active Sessions</h1>
        
        {{#if sessions.length}}
            <div class="row">
                {{#each sessions}}
                    <div class="col-md-6 mb-4">
                        <div class="card">
                            <div class="card-body">
                                <h5 class="card-title">{{user.name}}</h5>
                                <h6 class="card-subtitle mb-2 text-muted">{{project.name}}</h6>
                                
                                <div class="mt-3">
                                    <strong>Session Start:</strong> 
                                    <span>{{startTime}}</span>
                                </div>
                                
                                {{#if breaks.length}}
                                    <div class="mt-2 text-warning">
                                        <strong>Currently on break</strong>
                                    </div>
                                {{/if}}
                                
                                <a href="/admin/sessions/{{user.id}}" class="btn btn-primary mt-3">View Details</a>
                            </div>
                        </div>
                    </div>
                {{/each}}
            </div>
        {{else}}
            <div class="alert alert-info">
                No active sessions found.
            </div>
        {{/if}}
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 